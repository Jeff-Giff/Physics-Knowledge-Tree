# 知识树结构改造说明（一）：先修 / 相关 二元关系

本次改动落实第一个要求：**简化关联性结构**——移除 `superseded_by` / `supersedes`（上位替代）与 `links[].weight`（1~5 权重等级），代之以二元关系：`prereq`（先修）与 `related`（相关）。内容文件（`content/**/*.md` 的正文与具体连线）本次**未改动**。

## 新的节点格式（front-matter）

```yaml
links:
  - {id: calculus, type: prereq, note: 微积分是运动学与动力学的数学语言}
  - {id: wave-mechanics, type: related, note: 波动是力学系统的集体运动}
```

- `type` 必填，仅两种取值：
  - `prereq`：**先修，有方向**。在节点 A 中写 `- {id: B, type: prereq}` 表示"**B 是 A 的先修**"（学 A 之前应先掌握 B）。图上渲染为蓝色带箭头连线，箭头指向先修节点。
  - `related`：**相关，无方向**。渲染为普通灰色连线。
- 先修关系**只由"后续方"声明一次**即可，构建器自动为双方生成连线（详情页中对方会显示在"后续知识"分组）；不要双向重复声明，更不要互相声明（循环先修会报警告）。
- `related` 建议两边都写（note 可不同）；只写一边也能成边。
- 同一对节点一边写 `prereq`、一边写 `related`：按 `prereq` 处理并报警告。
- 旧字段 `superseded_by` / `supersedes`、旧写法 `weight`：构建时忽略并给出明确的迁移提示（按你的要求，build.py 只认新格式）。

## 修改的文件（共 12 个）

| 文件 | 修改内容 |
|---|---|
| `build.py` | 核心改造：links 解析改为 `type: prereq/related`（缺 type 或带 weight 即警告并跳过）；supersede 字段改为废弃警告；边构建支持 prereq 方向（source=后继、target=先修）、去重、循环先修检测；可视化剪枝改为"先修优先"；节点体积简化为 `18 + 10×度数`；输出 graph.js 中节点/边以 `type`/`dir` 取代 `weight`/`supersede`；统计输出改为"先修 X / 相关 Y"。 |
| `content/_meta.yaml` | `link_weight_semantics`（5 级权重说明）替换为 `relation_semantics`（prereq/related 语义说明）。 |
| `site/js/common.js` | `WEIGHT_LABEL`（5 级）→ `REL_LABEL`（先修/相关）。 |
| `site/js/detail.js` | 删除"上位替代"两个横幅与 supersedeLinks 区块；连线分组改为"先修知识 / 后续知识（以本节点为先修）/ 相关知识点 / 更远的关联"，卡片标签显示 先修·后续·相关。 |
| `site/js/app.js` | 3D/2D 渲染：prereq 边蓝色 `#58a6ff` + 箭头 + 方向粒子，related 边灰色；连线距离改为按类型取固定值（3D：先修 34 / 相关 56；2D：先修 80 / 相关 120 / 软连接 150）；顶栏统计改为"节点 N · 先修 P · 相关 Q"。 |
| `site/js/tree.js` | 学习树挂载判定由 weight≥4 改为按关系类型：先修边=强关联、相关边=弱关联。 |
| `site/js/challenge.js` | 挑战模式"严格"判定由 weight≥3 改为仅先修边。 |
| `site/index.html` | 挑战模式难度选项文案："严格（仅强关联）"→"严格（仅先修关系）"。 |
| `site/css/style.css` | 删除 `.sec-superseded`、`.link-card.supersede` 样式；新增 `.link-card.prereq`（蓝色描边）与 `.wtag.rel`（灰色标签）。 |
| `skills/.../SKILL.md` | 节点模板、字段语义表、连接管理规则、日志示例、校验说明、常见错误表全部改写为二元关系版本。 |
| `AGENTS.md` | 编辑规则第 2/5 条与告警类型清单更新。 |
| `README.md` | 特性描述、编辑说明、规模统计行更新。 |

`site/data/graph.js` 不在补丁中（它由 build.py 生成，内容迁移完成后重新构建即可）。

## 如何应用

在你的本地克隆中任选其一：

```bash
# 方式一：应用补丁
cd Physical-Knowledge-Tree
git apply 物理知识树-先修相关二元关系改造.patch

# 方式二：直接从我修改好的副本里复制上述 12 个文件覆盖
```

## 验证结果

- 用新格式小样（含先修/相关/循环先修/声明不一致/旧格式混排）测试 build.py：边方向、去重、各类警告全部符合预期。
- 对当前仓库（内容仍为旧格式）全量构建：正常完成，164 节点；所有旧 weight 链接与 superseded_by 字段逐条给出迁移提示（约千余条警告，属预期——内容迁移完成后应回到 0 警告）。
- 全部 JS 文件通过语法检查。

## 后续（内容迁移时）

把每个节点的 `links` 条目逐个改写：删掉 `weight`，按语义选择 `type`——"学这个之前必须先学那个"→ `prereq`（只写在后续方），其余 → `related`；`superseded_by`/`supersedes` 字段删除，其中有价值的信息（适用边界等）可保留在正文"深化内容/与其他知识的联系"中。迁移完运行 `python build.py` 至 0 警告。

---

# 第二步：节点体系替换——本科课程 + 研究方向（2026-08-13）

## 分类依据

- **课程层**：北大物理学院（普通物理五门 + 四大力学 + 数学物理方法 + 计算物理 + 实验五级次）与清华物理系（普通物理三个系列 → 四大力学 → 高年级四门专业限选）本科培养方案。
- **研究层**：arXiv Physics 顶层类别归并（astro-ph、gr-qc、hep-*、nucl-*、cond-mat.*、quant-ph、physics.atom-ph/optics/plasm-ph/flu-dyn/ao-ph/geo-ph/space-ph/bio-ph/med-ph、nlin.*）。

## 新体系：17 个类别、81 个节点

- **课程层（`c-` 前缀，暖色，5 子类 29 节点）**：数学基础（4）、普通物理（5）、四大力学（4）、实验与计算（4）、专业选修（12）。
- **研究层（`r-` 前缀，冷色，12 类 52 节点）**：天体物理与宇宙学（6）、引力与量子引力（4）、高能物理（4）、核物理（4）、凝聚态物理（6）、量子物理与量子信息（5）、原子分子与光物理（5）、等离子体物理（4）、流体与非线性科学（4）、统计物理与复杂系统（4）、软物质与生物物理（3）、地球大气与空间物理（3）。

连线原则：课程先修链（数学基础 → 普通物理 → 四大力学 → 专业选修 → 研究方向入口课）用 `prereq`，只由后续方声明；研究方向间真实交叉用 `related`，只声明一侧。

## 改动范围

- `content/`：原 22 个物理分支领域、164 个知识点节点全部删除，替换为上述 81 个节点（正文为"课程概述/主要内容/先修与后续"或"方向概述/核心问题/代表方法与设施/与其他方向的联系"的概述式骨架，含 arXiv 类别标注）。
- `content/_meta.yaml`：domains 替换为 17 个新类别。
- `site/data/graph.js`：重新生成（0 警告）。
- `README.md`、`AGENTS.md`、`skills/physics-knowledge-tree/SKILL.md`：同步改写（SKILL 的节点模板改为课程/研究方向两套）。
- `logs/2026-08-13.md`：追加本次改造日志。

## 验证

- `python build.py`：**0 警告**；节点 81，边 153（先修 107 / 相关 46）。
- 无头浏览器实测：无 JS 错误，顶栏统计显示"节点 81 · 先修 107 · 相关 46"，云图与新配色正常。

## 补丁使用

第二个补丁 `物理知识树-课程与研究方向节点.patch` 是**增量补丁**，需先应用第一个补丁：

---

## 第三步：箭头方向反转、高亮扩展、移除挑战模式

对应补丁 `物理知识树-箭头反转与高亮扩展.patch`（增量，基于第二步）。

### 1. 先修箭头指向反转（知识流向）

- 原来：先修边 `source=后继 → target=先修`，箭头指向先修节点。
- 现在：先修边 `source=先修 → target=后继`，箭头按**知识流向**渲染——高等数学 → 力学 → 理论力学 → …… → 研究方向。
- 改动点：`build.py` 输出边时交换 source/target（数据声明语义不变，仍由后续方声明先修）。

### 2. 单击高亮范围扩展

单击节点的高亮集合从"仅直接相连"扩展为三部分（`app.js` 的 `computeHighlightSet`）：

1. **直接相连**节点（任意关系）；
2. 沿先修关系**向下一级**（后继的后继）；
3. 沿先修关系**向上追溯到顶端**（如理论力学 → 力学 / 数理方法 → 微积分 / 线性代数）。

3D 与 2D 视图的节点、连线、标签统一按该集合高亮（连线需两端都在集合内）。图例与 2D 提示文案同步更新。

### 3. 移除挑战模式

- 删除 `site/js/challenge.js`；
- `index.html`：删除"挑战模式"按钮、挑战覆盖层（`#challenge` 整块）、`challenge.js` 脚本引用；
- `style.css`：删除挑战模式样式段；
- README 与 SKILL 文档中的挑战模式条目同步删除。

### 4. 附带修复：2D 视图节点被详情面板遮挡无法点击

- 问题：2D 画布铺满全宽，右侧详情面板压在上面，漂移到面板下方的节点收不到点击——表现为"2D 里单击高亮不生效"。
- 修复：`#graph2d` 增加 `right: 440px` 避开面板（小屏仍为 `right: 0`），提示条居中基准同步；`showEgo2D` 进入时显式同步画布尺寸（force-graph 只在窗口 resize 时自动重测，纯 CSS 变化不触发）。
- 说明：2D 是自我中心视图（仅中心 + 直接邻居），单击**中心**节点时所有可见节点都在其高亮集合内、画面无变化，属预期；单击其他节点即可看到集合外节点变暗。祖先链/子树的完整延伸主要体现在 3D 全局视图。

### 验证

- `python build.py`：0 警告，节点 81 / 先修 107 / 相关 46 不变；
- 脚本验证高亮集合：理论力学 → 向上含微积分、线性代数（追溯到顶端），向下含电动力学、广义相对论等后继链；
- Playwright 真实点击测试：2D 中单击量子力学 → 高亮集合 38 节点（含微积分），面板下方不再有节点，集合外节点变暗；
- headless 浏览器冒烟测试无 JS 错误。

---

## 第四步：着色方案调整

对应补丁 `物理知识树-着色方案调整.patch`（增量，基于第三步）。

- **课程层**（5 类）：低饱和度偏灰（S≈0.16~0.24），数学基础为灰紫色，其余为蓝紫→蓝绿区间内的灰调色；
- **研究层**（12 类）：高饱和度（S=0.82），色相自蓝紫（250°，天体物理与宇宙学）渐变至蓝绿（171°，软物质与生物物理 / 地球、大气与空间物理），相邻类别明度交替（0.58 / 0.47）保证可区分；
- **学科色相区间约定**（写入 `_meta.yaml` 注释与 SKILL 第 8 节，便于后续扩展）：数学 = 紫色（约 275°）；物理 = 蓝紫至蓝绿（250°→170°）；化学 = 绿至黄绿（120°→80°，预留，当前未启用）。

改动文件：`content/_meta.yaml`（17 个领域色值 + 注释）、`README.md`、`skills/physics-knowledge-tree/SKILL.md`、`logs/2026-08-13.md`，以及重新生成的 `site/data/graph.js`。

---

## 第五步：学科扩展——数学（紫）、化学（绿）、生命科学（黄绿-黄）

对应补丁 `知识树-数学化学生命科学扩展.patch`（基于 Jeff-Giff/Physics-Knowledge-Tree 当前 main）。

### 分类标准

- **数学**：课程按北大数院培养方案（数学分析/高等代数/解析几何 → 实变/复变/泛函/抽象代数/拓扑/微分几何 → 数值分析/偏微分方程）；研究按 arXiv math 顶层类别归并，参照教育部 0701 二级学科分为"基础数学"与"应用与计算数学"两个领域。
- **化学**（无 arXiv）：课程按四大化学 + 结构化学/仪器分析/高分子（北大化院方案）；研究按教育部 0703 二级学科（无机/分析/有机/物化/高分子）+ GB/T 13745（材料化学、化学生物学、量子化学与计算化学、能源与环境化学），分为"基础化学研究"与"交叉与应用化学"。
- **生命科学**：课程按北大生科院方案；研究按 arXiv q-bio 十个类目归并为"分子、细胞与基因组"与"个体、神经与生态"两个领域。

### 新增

- 9 个领域、64 个节点（数学 27 / 化学 17 / 生命科学 20），节点与连边按既有二元关系规则声明；
- 跨学科连边：化学 ← 微积分/热学/量子力学，生命科学 ← 有机化学（生物化学），数学 ↔ 物理（微分几何↔广义相对论、群论↔抽象代数、泛函分析↔量子力学等）；
- 附带补回 2D 面板遮挡修复（此前未合入的 style.css + app.js 改动）。

### 验证

- `python build.py`：0 警告；节点 145（课程 59 + 研究 86），边 289（先修 200 / 相关 89）；无孤立节点。

---

## 第六步：学科扩展（二）——信息科学、计算机科学、工程科学（紫红-粉红）

对应补丁 `知识树-信息计算机工程扩展.patch`（基于第五步之后的树）。

### 分类标准

- **计算机科学**：课程按 CS 本科核心（程序设计 → 数据结构/离散数学 → 组成原理/操作系统/网络/编译 → 算法/数据库/AI/机器学习）；研究按 arXiv cs 归并为"理论与算法（cs.DS/CC、cs.PL/FL/LO）/ 系统、网络与安全（cs.OS/AR/DC、cs.NI、cs.DB、cs.CR）/ 人工智能（cs.AI/LG/CV/CL/RO/GR/HC）"。
- **信息科学**：课程按电子信息类核心（信号与系统 → 数字信号处理/通信原理/信息论基础/数字图像处理）；研究按 arXiv eess（SP/IV/AS）+ 教育部 0810 信息与通信工程、0809 电子科学与技术。
- **工程科学**：课程按工科基础平台课（电路原理/电子技术/工程力学/材料科学基础/自动控制原理/工程热力学）；研究按教育部工学 08 大类二级学科（0802 机械、0805 材料、0807 能动、0808 电气、0811 控制、0814 土木、0825 航空宇航）归并为"机械、电气与控制"与"材料、能源与土建"。

### 新增

- 9 个领域、45 个节点：计算机 11 课程 + 12 研究；信息 5 课程 + 4 研究；工程 6 课程 + 7 研究；
- 跨学科连边：工程力学 ← 物理力学、材料科学基础 ↔ 固体物理/普通化学、工程热力学 ← 热学、密码学 ↔ 数论/量子通信、控制 ↔ 动力系统、能源工程 ↔ 磁约束聚变、微电子 ↔ 半导体物理等。

### 验证

- `python build.py`：0 警告；节点 190（课程 81 + 研究 109），边 370（先修 255 / 相关 115）。

### 应用方式

```bash
cd Physics-Knowledge-Tree
git apply 知识树-数学化学生命科学扩展.patch       # 第五步（若已应用请跳过）
git apply 知识树-信息计算机工程扩展.patch         # 第六步
python build.py   # 确认 0 警告
```
